name: simple shell commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: eccho a string
        run: echo "Hello World"
      - name: running multiline script
        run: |
          node -v
          npm -v